# BookBot
Boot.div guided project*** 
BookBot is a simple program that takes a text file *from the path provided in main.py* and gives a report on word count, and how many each letter was repeated throughout the document.
